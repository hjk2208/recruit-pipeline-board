# 요구사항 정리

TASK.md를 인수 기준으로 옮긴 것. 체크박스는 단위가 끝날 때 검증한 것만 채우고, wrap-up에서 전수 재검증한다.

## 필수 (Must)

### M1. 보드 / 컬럼 / 카드
- [x] 5개 컬럼이 `서류검토 → 면접 → 처우협의 → 최종합격 / 불합격` 순서로 보인다
- [x] 각 카드에 이름·직무·지원일·현재 단계가 보인다
- [x] 카드는 자기 단계의 컬럼에만 있다
- [x] 모바일 폭에서는 컬럼이 가로 스크롤(스냅)로 보이고 레이아웃이 깨지지 않는다

### M2. 단계 이동 (셀렉트)
- [ ] 카드 안 셀렉트에서 다른 단계를 골라 이동할 수 있다 (드래그 아님)
- [ ] 이동 후 새로고침해도 이동된 상태가 유지된다
- [ ] 이동 중인 카드는 진행 중임을 알 수 있다

### M3. 낙관적 업데이트
- [ ] 이동을 누르면 API 응답 전에 카드가 목표 컬럼으로 즉시 옮겨진다
- [ ] API 실패 시 카드가 원래 컬럼으로 돌아온다
- [ ] 실패 시 사용자에게 알림(토스트)이 보인다
- [ ] 실패 후 다시 이동을 시도할 수 있다
- [ ] 롤백 로직에 단위 테스트가 있다

### M4. 검색 / 필터
- [ ] 이름 입력 시 부분 일치로 카드가 걸러진다 (대소문자 무시)
- [ ] 직무 필터로 특정 직무만 볼 수 있다
- [ ] 검색 + 필터가 동시에 적용된다
- [ ] 250건에서 타이핑 중 입력이 끊기지 않는다

### M5. 상세 보기
- [ ] 카드 클릭 시 상세가 열린다 — PC(≥768px)는 사이드 패널, 모바일은 전체 화면 시트
- [ ] 닫기 버튼 / Esc로 닫힌다
- [ ] PC에서는 패널이 열린 채로 보드가 보이고 다른 카드를 클릭해 내용을 바꿀 수 있다

### M6. 로딩 / 에러 / 빈 상태
- [x] 초기 로딩 중 스켈레톤(또는 로딩 표시)이 보인다
- [ ] 초기 로딩 실패 시 에러 메시지 + 재시도 버튼이 보이고, 재시도가 동작한다
- [ ] 검색/필터 결과가 없으면 "결과 없음"이 보인다
- [ ] 컬럼에 카드가 없으면 빈 컬럼 안내가 보인다

### 제약
- [x] mock API 모든 호출에 200~800ms 지연이 있다
- [x] mock API 쓰기 호출이 약 15% 확률로 실패한다
- [x] 지연·실패율을 테스트에서 주입해 결정적으로 만들 수 있다
- [x] 데이터가 localStorage에 저장되어 새로고침 후 유지된다

## 설계 원칙
- **API 계층 분리** — 화면·훅은 API 인터페이스(`fetchCandidates`, `moveCandidate` 등)만 의존하고, mock은 그 인터페이스의 한 구현체다. 실제 백엔드가 붙으면 구현체만 교체한다. 지연·실패율은 mock 구현체 안에만 존재한다.

## 추가 요구사항 (Should) — Must 완료·검증 후에만

### S1. 웹 접근성 (키보드)
- [ ] Tab으로 카드 간 이동, Enter로 상세 열기
- [ ] 키보드만으로 단계 이동 컨트롤을 열고 선택할 수 있다
- [ ] 패널 열림/닫힘 시 포커스가 적절히 이동·복귀한다
- [ ] 토스트가 스크린리더에 읽힌다 (aria-live)

### S2. 경쟁 상태
- [ ] 같은 카드를 빠르게 2번 이동해도 최종 상태가 마지막 이동과 일치한다
- [ ] 앞 요청 실패 + 뒤 요청 성공 조합에서 데이터가 깨지지 않는다

### S3. Undo
- [ ] 이동 직후 "되돌리기"로 이전 단계로 복귀한다 (이것도 persist)

### S4. 대량 데이터
- [ ] 1,000건에서 스크롤·필터가 버벅이지 않는다

## 안 함
- 드래그앤드롭 — 키보드 대체 경로까지 구현하면 비용 2배. 버튼 방식이 접근성을 기본 확보.
- 카드 생성·삭제·편집 — 요구사항에 없음.
- 라우팅 — 페이지 하나.

## 결정된 세부 사항
요구사항에 없어 정한 것(이동 컨트롤 형태, 상세 보기 형태, seed 규모, 직무 목록, 날짜 형식, 실패율 배분, 반응형 범위)은 DECISIONS.md 참조.
