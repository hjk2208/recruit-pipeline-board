# 요구사항

## 배경

채용 담당자가 지원자를 채용 단계별로 관리하는 **채용 파이프라인 보드**. 단계는 `서류검토 → 면접 → 처우협의 → 최종합격 / 불합격` 이고, 지원자 카드를 단계 간 이동하며 관리한다. 데이터는 mock API를 통해 읽고 쓴다. 실제 백엔드는 없다.

## 필수 (Must)

1. **보드 / 컬럼 / 카드** — 단계별 컬럼에 지원자 카드를 표시. 카드에는 이름·직무·지원일·현재 단계.
2. **단계 이동** — 카드를 다른 단계로 이동(드래그앤드롭 또는 명시적 액션 버튼 중 택1). 이동은 mock API에 저장되어 새로고침 후에도 유지.
3. **낙관적 업데이트** — 이동 시 UI를 먼저 반영하고, API 실패 시 원상 복구 + 사용자 피드백.
4. **검색 / 필터** — 이름 검색 + 직무별 필터. 데이터가 200건 이상이어도 체감 지연 없이 동작.
5. **상세 보기** — 카드 클릭 시 지원자 상세(사이드 패널 또는 모달).
6. **로딩 / 에러 / 빈 상태** — 세 가지 상태를 모두 UI로 처리.

## 추가 요구사항 (Should)

- **경쟁 상태 처리** — 같은 카드를 빠르게 연속 이동해도 데이터가 깨지지 않아야 함.
- **대량 데이터 성능** — 1,000건 기준으로 스크롤/필터가 부드러워야 함(가상 스크롤 등).
- **되돌리기(Undo)** — 방금 한 이동을 취소.
- **테스트** — 핵심 로직(낙관적 업데이트 롤백 등)에 대한 테스트.
- **웹 접근성** — 키보드만으로 단계 이동과 상세 열람이 가능해야 함. 드래그앤드롭을 택했다면 키보드 대체 경로 필수.

## 기술 제약

- 스택 자유 (React, Next.js, 상태관리·스타일링).
- **mock API는 자체 구현**하며, 네트워크 지연(200~800ms)과 실패(약 15%)를 반드시 시뮬레이션할 것.
- 외부 UI 라이브러리 사용 가능. 단 핵심 상태 로직을 통째로 대신해 주는 라이브러리에 의존한 경우 그 선택 이유를 문서에 밝힐 것.
