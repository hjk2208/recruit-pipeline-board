# 프롬프트 & 리뷰 로그

기능 단위마다 한 섹션. 커밋 히스토리와 1:1로 대응한다. 프롬프트는 요약하지 않고 원문을 남긴다. 리뷰·검증에는 실제로 발견하고 판단한 것만 적는다.

---

## [harness] AI 협업 규칙 · 스킬 세팅

### 프롬프트 1

```
채용 파이프라인 보드 만들 건데, 코드 짜기 전에 CLAUDE.md부터 만들어줘.
기능 요구사항은 다음에 따로 줄 거야. 지금은 규칙이랑 스킬만.

스택은 Vite + React + TS, Tailwind 4, 서버 상태는 TanStack Query. 컴포넌트 상태는 useState로 하고, 여러 컴포넌트가 공유해야 할 때만 zustand. 서버 데이터를 zustand에 넣지 마.
Next를 쓰지 않는 이유는 페이지 하나짜리고 SSR/라우팅 쓸 일이 없어서.

꼭 지켜야 되는 제약:
- mock API 직접 만들어야 함. 지연 200~800ms, 실패 15% 정도. 새로고침해도 데이터 남아있어야 함
- 단계 이동은 DnD 말고 버튼으로. 키보드 접근성 때문에
- 낙관적 업데이트랑 실패 시 롤백이 핵심 요구사항이라 이건 라이브러리에 맡기지 말고 직접 짜야 돼

작업 방식:
- 한 번에 기능 하나씩. 내가 여러 개 시켜도 쪼개서 순서 제안하고 하나씩
- 짜고 나면 바로 타입체크/린트/테스트/빌드 돌려서 확인. 확인 안 하고 넘어가지 마
- 핵심 로직은 테스트 먼저
- 애매한 건 나한테 물어봐. 선택지랑 트레이드오프 같이 줘. 결정은 내가 하고, 그 가정을 DECISIONS.md에 적어
- 라이브러리 깔기 전에 npm view로 버전 확인

커밋은 기능 하나당 하나. `type(scope): 요약` 형식이고 본문에 뭘 왜 했는지랑 AI 초안 어디 고쳤는지 적어.
squash나 force-push는 하지 마.

기록:
- PROMPTS.md: 기능별로 프롬프트 원문 / 출력 요지 / 리뷰·검증 / 판단. 그 기능 커밋에 같이 넣어
- DECISIONS.md: 결정 나올 때마다 바로 한 줄씩. 몰아서 쓰지 마

스킬도 같이 만들어줘. .claude/skills/ 아래에
- verify: 타입체크/린트/테스트/빌드 한 번에 돌리고 결과 보고
- unit-done: 기능 하나 끝났을 때 verify → OCR delegate-review로 리뷰(자동 수정 말고 리포트만, 고칠지는 내가 정함) → PROMPTS.md 섹션 초안 → DECISIONS 갱신 → 커밋 메시지 초안. 커밋은 내가 확인하고, 커밋하면 바로 push
- wrap-up: 마지막 정리용. 인수기준 검증, README, DECISIONS 빈 곳 채우기

CLAUDE.md랑 스킬 만들고 chore(harness) 커밋까지 해줘.
```

### 프롬프트 2

```
흠, 중요한 DIFF가 있으면 HTML로 보여달라고 하는걸 하네스에 넣으면 어때?
```

### AI 출력 요지

- (프롬프트 2) "중요한"의 기준을 제안하고 반영 — 핵심 로직(mock API·낙관적 업데이트·스토어)은 항상, 그 외 50줄 이상 또는 파일 3개 이상. `unit-done` 2단계로 삽입, CLAUDE.md 소통 절에 한 줄.
- `CLAUDE.md` — 스택(확정) / 제약(타협 불가) / 구현 리듬 / 기록 / 커밋 / 코드 스타일 / 소통 7개 섹션. 프롬프트의 규칙을 그대로 옮기되, 코드 스타일(Suspense·ErrorBoundary 경계, 파일 책임 분리, Tailwind 토큰, focus 링 유지)은 AI가 보강.
- `.claude/skills/verify` — 4단계를 각각 따로 실행하고 표로 보고. 하나 실패해도 나머지 마저 실행.
- `.claude/skills/unit-done` — 검증 → OCR delegate 리뷰(High/Medium만, 자동 수정 없음, 채택은 사용자) → PROMPTS 초안 → DECISIONS → 커밋 메시지 초안 → 확인 후 커밋·push.
- `.claude/skills/wrap-up` — 인수 기준 전수 검증, README, DECISIONS/PROMPTS 점검(커밋 로그와 1:1 대조), 최종 verify.
- `DECISIONS.md` — 이 프롬프트 이전 대화에서 내린 결정 8개를 소급 기록(기간 해석, Vite, TanStack Query, zustand 조건, 버튼 이동, mock API 모듈, OCR 교차 리뷰, 판단 주체).

### 리뷰 / 검증

- 이 프롬프트가 나오기까지 AI 초안 규칙을 두 번 고침:
  - 문제: AI가 "모호하면 스스로 가정하고 DECISIONS에 기록" 규칙을 제안 → 설계 판단의 주체가 AI가 됨. 알아챈 방법: 프롬프트 초안 정독. 판단: **수정** — "선택지·트레이드오프 제시 후 사용자가 결정"으로.
  - 문제: 서버 상태를 직접 스토어로 관리하자는 AI 초기 제안 → 5시간 안에 로딩·에러·재조회까지 수작업은 버그 위험. 판단: **기각**, TanStack Query + 롤백 로직만 직접 작성.
- OCR delegate 모드 동작 확인: `ocr delegate preview`가 워킹트리 12개 파일을 인식함. API 키 없이 동작.
- 스킬이 참조하는 `pnpm typecheck/lint/test/build` 스크립트는 `chore(setup)` 커밋에서 추가됨. 실행 결과는 다음 단위부터 기록.

### 판단

규칙 파일과 기능 요구사항을 분리(요구사항은 다음 프롬프트). 커밋 후 push를 unit-done에 고정 — 타이머 안 작업이라 로컬에만 두는 위험을 없앰.

---

## [plan] 요구사항 정리 · 작업 단위 분해

### 프롬프트 1

```
요구사항 정리한 거 줄게 (TASK.md). 인수 기준으로 정리하고 기능 단위로 쪼개서 순서 제안해줘. 필수 먼저, 추가 요구사항은 뒤로.
```

### 프롬프트 2

```
실제 백엔드가 붙었을 때 교체 가능하게끔 확장성도 고려한다고 적는게 어때?
```

### 프롬프트 3

```
이동 컨트롤은 셀렉트로 가는 거 동의하고, 상세 보기는 UX적인 측면도 생각하면 어떤 것 같아? seed는 250건 구현해보고, 추가 요구사항 구현시에 1000건으로 늘리자
```

### 프롬프트 4

```
상세 구현 방법은 이게 모바일에서 보는가 PC에서 보는가가 다를 것 같은데, 담당자는 PC가 우선일 것 같으니 PC 우선으로 생각하자
```

### AI 출력 요지

- REQUIREMENTS.md — Must 6 · Should 4 · 인수 기준 체크리스트 · 설계 원칙
- PLAN.md — 단위 11개 + 추가 요구사항 3개, 각 단위의 인수 기준과 검증 방법
- 요구사항에 없는 것(이동 컨트롤·상세 형태·seed 규모)은 선택지와 트레이드오프로 제시

### 리뷰 / 검증

- 문제: 백엔드 교체 가능성이 요구사항 정리에 빠짐. 알아챈 방법: 정독. 판단: **추가** — API 인터페이스 + mock 구현체 분리를 설계 원칙으로, PLAN 2번 단위에 반영.
- 문제: AI가 상세 보기를 "패널 vs 모달" 양자택일로 제시 — 뷰포트에 따라 답이 다름. 알아챈 방법: UX 동선 기준으로 다시 검토. 판단: **수정** — PC 패널·모바일 시트로 한 컴포넌트가 전환, 반응형은 PC 우선으로 범위 고정.
- 세 가지 선택은 AI 권장안을 그대로 받지 않고 트레이드오프를 보고 결정.

### 판단

- 이동 컨트롤: 카드 안 <select> / 상세: PC 사이드 패널·모바일 전체 화면 시트(같은 컴포넌트) / seed: 250건 → 추가 요구사항에서 1,000건
- 반응형은 PC 우선, 모바일은 컬럼 가로 스크롤로 깨지지 않는 수준까지
- 직무 목록은 요구사항에 없어 프론트엔드 / 백엔드 / 디자인 / PM / 데이터 5종으로 정함 — seed 데이터와 직무 필터 옵션에 공통 사용
- 지원일은 2026-09-11 형식(YYYY-MM-DD)으로 표시 — 정렬·판독이 가장 무난
- mock 실패율은 쓰기(이동) 15%, 조회 5% — 조회까지 15%면 첫 화면이 자주 깨져 작업 흐름이 끊김. 에러 상태는 재시도 버튼으로 검증

---

## [mock-api] API 계약 · mock 구현체

### 프롬프트 1

```
mock-api 작업 시작하자. 바로 짜지 말고 어떤 API를 만들어야 할지 먼저 같이 설계하자.

화면에서 필요한 건 목록 조회랑 단계 이동 정도인 것 같은데, 인터페이스 초안 잡아주고
갈리는 지점 있으면 선택지랑 트레이드오프로 보여줘. 특히 낙관적 업데이트 롤백이랑 엮이는 부분은 미리 짚어줘.
실제 백엔드로 교체 가능해야 하는 거 잊지 말고.
```

### AI 출력 요지

- 설계: `CandidateApi { fetchCandidates, moveCandidate }` 인터페이스 + `index.ts` 교체 지점. 상세 조회는 별도 호출 없이 목록에 포함. 갈리는 지점 4개를 선택지로 제시 — 반환값(지원자 하나 vs 전체), 에러 형태(구조화 vs 메시지), 지연(호출마다 랜덤 vs 고정), 롤백 단위(항목 vs 전체 스냅샷).
- 구현: `types / contract / errors / index / mock/seed / mock/createMockApi` 6개 파일. 지연·실패율·난수·저장소·seed·시계 전부 주입 가능. 고정 시드 PRNG로 250건 결정적 생성.
- 테스트 17개: 조회·불변성·이동·영속(새 인스턴스로 재조회)·NOT_FOUND·reset·깨진 저장소 / 실패율(쓰기 100%, 난수 수열 주입, 조회·쓰기 분리) / 지연(fake timers로 경계값).

### 리뷰 / 검증

- 설계 단계에서 잡은 것: 일반적인 TanStack Query 예제처럼 목록 전체 스냅샷으로 롤백하면, 카드 A 이동 중 카드 B 이동 → A 실패 시 B의 낙관적 상태까지 지워짐. 알아챈 방법: 롤백 시나리오를 손으로 그려봄. 판단: **항목 단위 롤백**으로 확정 — 이건 추가 요구사항(경쟁 상태)이 아니라 Must 3이 깨지는 케이스.
- 테스트로 검증: 실패 판정이 난수 수열대로 결정적으로 갈리는지, 기본 지연 상한 800ms 경계에서 799ms엔 안 끝나고 800ms에 끝나는지, 실패한 이동이 저장되지 않는지.
- HTML diff 리뷰: 파일 8개 정독, 코드 지적 없음.
- OCR 교차 리뷰(delegate): Medium 2건, Low 3건. ① 에러·빈 상태를 재현할 스위치가 없음 → **9번 단위로 이관**(PLAN에 메모). ② `to` 런타임 검증 없음 → **기각**, 호출자가 전부 타입 안전. Low는 버림.

### 판단

- 반환값 지원자 하나 / 에러 `ApiError { code, status, retryable }` / 지연 호출마다 랜덤 + 주입 / 롤백 항목 단위
- AI 초안 그대로 채택, 코드 수정 없음

---

## [board-layout] 5단계 컬럼 레이아웃 · TanStack Query 연결

### 프롬프트 1

```
board-layout 가자. 5단계 컬럼 레이아웃만 먼저.

- TanStack Query 붙여서 fetchCandidates 연결, 로딩은 Suspense 폴백(스켈레톤)으로
- 컬럼 헤더에 단계명이랑 건수
- 카드는 아직 안 그려도 돼, 다음 단위에서
- 모바일에선 컬럼 가로 스크롤

폴더 구조 어떻게 잡을지 먼저 보여주고 시작해.
```

### AI 출력 요지

- 폴더 구조안 2개(기능별 vs 평평한 구조) 제시 → 기능별로 확정. `app/App.tsx · queryClient.ts`, `features/board/{Board, BoardColumn, BoardSkeleton, queries, groupByStage, useCandidates}`.
- `queryOptions`로 key·fetch를 한 곳에 두어 이후 이동·무효화가 같은 key를 쓰게 함. `retry: false` 제안.
- 컬럼은 `region` + `aria-labelledby`, 스켈레톤은 `role=status`. 모바일은 flex + overflow-x + snap, md 이상은 5열 grid.
- 테스트 3개: groupByStage(빈 입력에도 다섯 키, 순서 유지), Board(스켈레톤 → 5 region → 컬럼별 건수).

### 리뷰 / 검증

- 문제: 데스크톱에서 페이지가 가로로 넘침 — 컬럼 `min-w-72`가 md 이상 grid에도 걸려 5열 합 1440px. 알아챈 방법: 브라우저 스크린샷에서 5열이 잘려 보여 `scrollWidth`(1078) > `clientWidth`(976) 측정. jsdom 테스트는 레이아웃을 못 잡음. 판단: **수정** — `md:min-w-0`. 재확인: 데스크톱 5열 185px 넘침 없음, 모바일(375px) 288px 가로 스크롤·스냅 정상.
- 문제: `groupByStage` 초기값을 `Object.fromEntries(...) as` 캐스트 → TS2352. 알아챈 방법: typecheck. 판단: **수정** — 다섯 키 명시 리터럴. 단계가 늘면 누락을 TS가 잡아서 캐스트보다 낫다.
- OCR 교차 리뷰: Medium 2건. ① Board·BoardSkeleton의 컨테이너 클래스 중복 → **유지**, 규칙대로 3번째에 추출. ② ErrorBoundary 부재로 조회 실패 시 빈 화면 → **계획대로 9번 단위**.
- HTML diff 리뷰: 파일 10개.

### 판단

- 폴더 구조 기능별(`features/<단위>/`), Query `retry: false`
- 컨테이너 클래스 중복은 3번째까지 유지, ErrorBoundary는 9번 단위

---

## [card-list] 컬럼 안 지원자 카드

### 프롬프트 1

```
card-list 가자. 컬럼 안에 지원자 카드 그려줘.

- 카드에 이름·직무·지원일·현재 단계. 단계는 컬럼에 이미 보이니까 카드에선 작은 배지 정도로
- 250건 전부 렌더해도 스크롤 버벅이면 안 돼. 컬럼별로 세로 스크롤
- 카드는 다음 단위에서 클릭이랑 키보드(Tab/Enter)로 상세 열 거니까 처음부터 포커스 가능한 요소로
- 셀렉트(단계 이동)는 5번 단위에서 붙일 거라 자리만 생각해두고 지금은 넣지 마
- 카드 컴포넌트는 features/candidate-card/ 로

브라우저에서 250건 스크롤 확인해서 알려줘.
```

### AI 출력 요지

- `features/candidate-card/{CandidateCard, StageBadge}`. 카드는 `li`, 이름이 `button`(포커스 대상), 직무·지원일은 `aria-describedby`로 버튼 설명에 연결. 배지는 최종합격/불합격만 의미색.
- 컬럼 슬롯을 `ul` + `overflow-y-auto`로, App을 `h-screen`으로 고정해 컬럼이 내부 스크롤. `min-h-0`가 없으면 flex 자식이 내용만큼 늘어나 스크롤이 안 생긴다는 점을 주석으로.
- 테스트 3개 추가(카드 4필드, 이름 버튼 포커스+설명, 카드가 자기 컬럼에만).
- 브라우저 측정: 카드 250개, 컬럼 5개 모두 내부 스크롤, 페이지 넘침 없음, 컬럼 스크롤 40프레임 평균 8.2ms·최대 9.3ms, Tab이 이름 버튼에 잡힘.

### 리뷰 / 검증

- 문제: "카드를 포커스 가능한 요소로"를 그대로 하면 카드 전체가 `button`이 되는데, 그러면 다음 단위의 `select`를 안에 넣을 수 없음. 알아챈 방법: 5번 단위 구조를 미리 그려봄. 판단: **수정 제안** — 이름 버튼을 포커스 대상으로, select는 형제. DECISIONS에 기록.
- 문제: 테스트 두 번째가 "버튼 2개 발견"으로 실패. 원인은 코드가 아니라 Vitest `globals: false`에서 Testing Library 자동 cleanup이 안 걸려 DOM이 누적된 것. 알아챈 방법: 첫 실행 실패 메시지의 매칭 요소 2개. 판단: **수정** — setup에 `afterEach(cleanup)`. 이전 단위는 파일당 테스트 1개라 드러나지 않았음.
- OCR 교차 리뷰: Medium 1건 — `StageBadge` tone 맵에 중립 톤 문자열 3번 반복 → **유지**, `Record<Stage, …>`라 단계가 늘면 누락을 TS가 잡는 이점이 더 큼. Low 2건 버림.
- HTML diff 리뷰: 파일 8개.

### 판단

- 포커스 대상 = 이름 버튼, select는 형제로 (사용자 확인)
- 250건은 memo·가상화 없이 충분 — 측정치 근거. 1,000건은 추가 요구사항 단위에서 재측정

---

## [stage-move] 셀렉트로 단계 이동 · persist

### 프롬프트 1

```
stage-move 가자. 카드에서 단계 옮기는 거.

- 카드 안에 셀렉트로 다른 단계 고르면 바로 moveCandidate 호출
- 성공하면 목록 갱신돼서 카드가 그 컬럼으로 가고, 새로고침해도 유지
- 이동 중인 카드는 진행 중인 게 보이게 (셀렉트 disabled 정도)
- 낙관적 업데이트는 아직. 6번에서. 지금은 응답 오면 옮기는 걸로
- 실패하면 일단 콘솔 에러만. 토스트도 6번에서
```

### AI 출력 요지

- `features/stage-move/useMoveCandidate` — `useMutation`, 성공 시 서버가 돌려준 지원자 하나로 캐시의 그 항목만 교체(mock-api 결정 ①). `StageSelect` — 네이티브 select, `aria-label`에 이름, 진행 중 disabled.
- 카드의 select는 이름 버튼과 형제(card-list 결정).
- 테스트 4개 추가: 진행 중 disabled → 응답 후 항목 교체(다른 항목 참조 유지) / 실패 시 캐시 불변 / Board에서 옮기면 컬럼·건수 변경 / 카드 셀렉트가 현재 단계.
- 브라우저: 정도윤 서류검토→면접, 진행 중 disabled 확인, 응답 후 면접 컬럼(68건), localStorage `면접`, 새로고침 후 유지.

### 리뷰 / 검증

- 문제: 실패 케이스 테스트가 `Error: boom`으로 죽음. 알아챈 방법: 이분법 — spy 제거·console.error 제거·단독 실행·retry 옵션 제거는 여전히 실패, **beforeEach의 `mockReset` 제거만 통과**. 같은 훅으로 대조 프로브 3개(거부 / 거부+캐시 / 성공) 전부 통과 → 훅은 정상, vitest 5의 `mockReset` 뒤 `mockImplementation(() => Promise.reject)` 상호작용. 판단: **수정** — beforeEach 제거, 테스트마다 구현 명시(원래 그랬음). 메커니즘은 시간상 추적 안 하고 주석으로.
- 문제: 셀렉트를 메타 옆에 두니 좁은 컬럼(185px)에서 직무·지원일이 3줄로 꺾임. 알아챈 방법: 브라우저 스크린샷. 판단: **수정** — select를 아래 줄 오른쪽 정렬.
- OCR 교차 리뷰: Medium 2건 — ① 이동 중 상태가 disabled뿐이라 보조기기엔 "비활성"으로만 읽힘 → **6번으로**(낙관적 갱신에서 표현 자체가 바뀜, aria-live 토스트도 그때). ② onError가 console뿐 → 프롬프트대로 **6번**. Low 2건 버림.
- HTML diff 리뷰: 파일 6개.

### 판단

- 응답 후 캐시 항목 교체 방식으로 이동 완료. 낙관적 갱신·토스트·aria-live는 6번에서
- 테스트 환경 이슈는 훅 정상 확인 후 테스트 쪽만 손봄

---

## [optimistic-update] 낙관적 반영 · 항목 단위 롤백 · 재시도 토스트

### 프롬프트 1

```
optimistic-update 가자. 제일 중요한 거.

- 셀렉트 고르면 응답 기다리지 말고 카드 바로 옮겨
- 실패하면 그 카드만 원래 컬럼으로 되돌리고 토스트로 알려줘. 토스트에 재시도 버튼
- 다른 카드가 이동 중이어도 서로 영향 없어야 돼 (mock-api 때 정한 항목 단위 롤백)
- 토스트는 스크린리더도 읽게
- 롤백 로직은 테스트 먼저. 성공 / 실패 / 두 장 동시 이동 중 하나 실패 케이스
```

### AI 출력 요지

- 테스트 먼저: `optimistic.test.ts` 5개(성공·없는 id·항목 롤백·두 장 동시 중 하나 실패·전체 스냅샷 방식과의 대조) → `optimistic.ts` 순수 함수 2개.
- `useMoveCandidate`: `onMutate`에서 cancelQueries → `applyStage` → 이전 단계를 컨텍스트로 / `onError`에서 `rollbackStage` + 토스트(재시도는 `retryable`일 때만) / `onSuccess`에서 서버값으로 교체. `onSettled` 무효화 없음.
- 토스트: zustand 스토어 + `Toaster`(`role=status aria-live=polite`, 비어 있어도 상시 렌더). 재시도·닫기 버튼.
- `StageSelect.test` 4개를 낙관적 동작으로 교체: 즉시 반영→서버값 / 실패 롤백+재시도 / 재시도 불가 에러 / 두 장 동시 중 하나 실패. 총 34개.
- 브라우저: 한 카드를 12번 왕복 — 12번 모두 응답 전 즉시 이동, 실제 실패 2회(15% 기대치 근처) 모두 원래 컬럼 복귀 + 토스트, 매번 화면 = localStorage.

### 리뷰 / 검증

- 검증(테스트): 전체 스냅샷 롤백이면 두 장 동시 이동에서 두 번째 카드의 낙관적 상태가 날아간다는 걸 대조 테스트로 고정. 이게 이 단위의 핵심 판단.
- 검증(브라우저): 실패는 15% 확률이라 12회 반복으로 2회 잡음. 실패율 스위치가 없어 반복에 의존 — 9번 단위 스위치의 필요성 재확인.
- OCR 교차 리뷰: Medium 3건. ① 같은 카드 연속 이동 시 두 번째 롤백이 첫 이동의 낙관적 값으로 감 → 요구사항의 "경쟁 상태" 항목 그 자체, **12번 단위로**. ② `onSettled` 무효화 없음 → **의도**, DECISIONS. ③ 에러 토스트 자동 닫힘 없음 → **의도**, 재시도 버튼이 있는 알림은 사용자가 닫는다. Low 2건 버림.
- HTML diff 리뷰: 파일 7개.

### 판단

- 항목 단위 롤백 + 순수 함수 분리 + 재조회 없음 (사용자 확인)
- 같은 카드 연속 이동은 12번, 토스트 정리는 9번

---

## [readme] 임시 README · 작업 범위 정정

### 프롬프트 1

```
README 임시로 써줘. 실행 방법, 스택, mock API 방식, 지금까지 된 것/남은 것. 나중에 wrap-up에서 다시 정리할 거야.
```

### AI 출력 요지

- README: 실행 명령, 스택, mock API 방식(인터페이스/구현체 분리·지연·실패율·영속·주입), 진행 현황 표, 문서 링크.
- DECISIONS의 "작업 범위" 줄을 정정 — 6시간 완결 가정이 담당자 확인으로 12일로 바뀜.

### 리뷰 / 검증

- 진행 현황 표를 REQUIREMENTS 체크 상태와 대조. "로딩·에러·빈 상태"를 ✅로 쓰려던 초안을 "로딩만"으로 고침 — 스켈레톤만 있고 에러·빈 상태는 없음.

### 판단

- wrap-up에서 구조 개요·안 한 것과 이유를 추가해 최종본으로

---

## [search-filter] 이름 검색 · 직무 필터

### 프롬프트 1

```
search-filter 가자. 보드 위에 검색이랑 필터.

- 이름 검색은 부분 일치, 대소문자 무시. 타이핑할 때 250건이어도 안 끊기게
- 직무 필터는 셀렉트, 전체/5종
- 둘 다 동시에 걸리게
- 필터링은 순수 함수로 빼서 테스트
- 검색 상태는 어디 둘지 판단해서 알려줘
```

### AI 출력 요지

- 검색 상태 위치 판단: Board 로컬 `useState`(쓰는 곳이 둘뿐) vs zustand(과함) → 로컬. 입력 즉시성은 `useDeferredValue`.
- `filterCandidates` 순수 함수 + 테스트 4개(빈 조건 시 원본 참조 유지·부분 일치/대소문자/공백·직무·동시). `SearchFilterBar`(`type=search`, `role=search`, stale 표시 aria-live). Board 테스트에 검색→필터 흐름 추가. 총 39개.
- 브라우저: "김서" → 1건, + 백엔드 → 0건. 입력 중 프레임 드랍 없음.

### 리뷰 / 검증

- OCR 교차 리뷰: Medium 2건 — ① 결과 0건일 때 안내 없음 → M6 항목이라 **9번 단위**. ② `useDeferredValue`는 필터링 횟수를 줄이지 않음 → **유지**, 250건은 무관하고 1,000건에서 재측정. Low 2건 버림.
- HTML diff 리뷰: 파일 5개.

### 판단

- 검색 상태 로컬, deferred value. 빈 결과 안내는 9번

---

## [detail-panel] 상세 패널 · 모바일 시트

### 프롬프트 1

```
디테일 패널 시작하자. 카드 이름 누르면 상세 나오는거.

- PC는 오른쪽 사이드 패널, 보드 보이는 채로. 모바일은 전체 화면 시트. 컴포넌트 하나로
- 내용은 이름·직무·지원일·단계·이메일·전화·메모. 패널 안에서도 단계 바꿀 수 있게
- 닫기 버튼이랑 Esc. 닫으면 눌렀던 이름으로 포커스 돌아가게
- 열린 채로 다른 카드 누르면 내용만 바뀌게
- 어느 카드가 열려있는지는 어디 둘지 판단해서 알려줘
```

### 프롬프트 2

```
1024px에서 패널이 보드를 밀어 컬럼이 눌리는 건 오버레이 방식으로 가자. 모바일 시트 포커스 트랩은 접근성 단위에서 하고.
커밋 전에 오버레이 상태를 브라우저에서 보고 싶어.
```

### 프롬프트 3

```
오버레이일 때 불합격 컬럼이 패널에 가려지는데 나머지 컬럼은 그대로 보이니까 "닫힌 건지 가려진 건지" 헷갈려.
오버레이로 나오는 경우엔 백드롭으로 배경을 어둡게 하고, 배경 클릭하면 닫히게 해줘.
1280 이상에서 보드 옆으로 밀릴 때는 트랜지션 넣어주고.
```

### 프롬프트 4

```
백드롭이 나타날 때도 트랜지션 있으면 좋겠어. 그러면 오버레이 자체도 트랜지션이 있어야 맞지.
```

### 프롬프트 5

```
모바일에서는 상세 패널 폭이 이상해. 오른쪽이 비고 닫기 버튼이 중간에 있어.
```

### AI 출력 요지

- 선택 상태 판단: zustand에 id만(카드와 패널이 트리상 멀어 공유 상태) vs Board 로컬 + prop(3단계 전달) → zustand. 서버 데이터는 넣지 않고 패널이 캐시에서 찾음.
- `DetailPanel`: `role=dialog aria-modal=false`, `fixed inset-0 → md:static w-80`, 열리면 닫기 버튼 포커스, Esc, `StageSelect` 재사용. `detailStore`: `open(id, opener)`/`close()`가 opener로 포커스 복귀, 열린 채 전환 시 최초 opener 유지.
- 테스트 4개(열기+포커스 / Esc+복귀 / 전환+최초 opener 복귀 / 패널 안 셀렉트). 총 43개.
- (프롬프트 2~4) 1280 미만 백드롭 + 오버레이 슬라이드 인, 1280 이상 폭 트랜지션, 모바일 슬라이드 업. 트랜지션을 위해 패널·백드롭 모두 마운트 유지, 닫힘은 translate/opacity + pointer-events-none.
- 브라우저: 375·1024·1440에서 위치·트랜지션 중간값·백드롭 클릭 닫기·Esc 후 포커스 복귀 측정.

### 리뷰 / 검증

- 문제: 패널을 붙이자 페이지가 가로로 넘침. 알아챈 방법: 브라우저 측정 `scrollWidth > innerWidth`. 원인: Board가 flex 행의 자식이 되면서 min-width:auto로 내용 폭(5×288)까지 늘어남. 판단: **수정** — Board 루트 `min-w-0`.
- 모바일 첫 스크린샷이 빈 화면으로 나와 확인 — DOM에는 내용이 있었고 h2 rect도 정상. 캡처 타이밍 문제였음. 재캡처로 정상 확인.
- OCR 교차 리뷰: Medium 2건 — ① 1024px에서 패널이 열리면 컬럼 118px로 눌림 → 선택지 A(오버레이)/B(가로 스크롤 전환)/C(유지) 제시, **A 채택**. ② 모바일 시트는 사실상 모달인데 포커스 트랩 없음 → **a11y 단위**. Low 2건 버림.
- 문제: A를 lg(1024) 경계로 넣었더니 1024에서 여전히 밀림 — 1024가 lg에 포함. 알아챈 방법: 사용자가 오버레이를 보자고 해서 1024로 측정. 판단: **수정** — xl(1280).
- 문제: 오버레이인데 뒤가 그대로 보여 "가려짐"과 "닫힘"이 구분 안 됨. 알아챈 방법: 사용자가 오버레이 상태에서 불합격 컬럼이 사라지는 걸 보고 지적. 판단: **수정** — 백드롭(클릭 닫기) + 슬라이드 인, 옆으로 밀릴 땐 폭 트랜지션. 사용자 후속 요청으로 백드롭·오버레이에도 트랜지션.
- 문제: 슬라이드 트랜지션이 안 걸림. 알아챈 방법: 60ms 시점 좌표가 최종값과 같음. 원인: Tailwind v4의 `translate-*`는 `transform`이 아니라 `translate` 속성 → `transition-[transform,…]`에서 빠짐. 판단: **수정** — `transition-[translate,…]`.
- 문제: 모바일에서 상세 폭이 280px로 고정돼 오른쪽이 비고 닫기 버튼이 중간. 알아챈 방법: 사용자가 모바일 화면에서 확인. 원인: xl 폭 트랜지션 중 내용이 찌그러지지 않게 둔 `w-70`이 모바일에도 적용. 판단: **수정** — `xl:w-70`.
- HTML diff 리뷰: 파일 6개.

### 판단

- id만 zustand, 포커스 복귀는 opener 기억
- 1280 미만 백드롭 오버레이 / 이상 옆으로, 셋 다 트랜지션. 모바일 포커스 트랩은 a11y 단위

---

## [loading-error-empty] 에러 경계 · 빈 상태 · mock 오버라이드

### 프롬프트 1

```
이제 로딩/에러/빈 상태 구현하자. 로딩 스켈레톤은 지금 있으니까 나머지에 대해서 작업 시작하자.

- 초기 조회 실패하면 에러 메시지랑 재시도 버튼. 재시도 누르면 다시 불러오기
- 검색 결과 0건이면 안내 문구, 카드 없는 컬럼엔 빈 컬럼 안내
- 에러 화면 확인하려면 실패율을 강제로 올릴 수 있어야 하니까 URL 쿼리로 mock 옵션 덮어쓰기 (?failRate=1 같은 거). 이건 개발용이라는 게 드러나게
- 에러 토스트가 같은 카드에서 여러 개 쌓이면 이전 건 정리
```

### AI 출력 요지

- `features/status/{ErrorBoundary, BoardError, EmptyState}`. App은 `QueryErrorResetBoundary > ErrorBoundary > Suspense` — 재시도가 캐시 에러를 지우고 다시 suspend.
- `api/mock/devOverrides.ts`: `?failRate` `?delay` `?seed` `?reset` → mock 옵션. `api/index.ts`에서 합성. 파일 주석에 용도·목록.
- Board: 결과 0건이면 컬럼 대신 안내(필터 중 / 데이터 없음 문구 분기), 컬럼별 0건은 compact 안내. 토스트 스토어에 `key` — 같은 카드 이전 토스트 정리.
- 테스트 8개 추가(실패→재시도→성공, 0건, 검색 0건+빈 컬럼, 오버라이드 파싱, 토스트 정리). 총 52.
- 브라우저: `?failRate=1` 에러+재시도 / `?seed=0` 빈 보드 / 검색 0건 안내 / 빈 컬럼 안내.

### 리뷰 / 검증

- 문제: 기존 Board 테스트 1개 실패 — 필터 0건이면 컬럼이 안 그려지는 새 동작. 알아챈 방법: verify. 판단: **테스트 갱신**(동작 변경이 의도).
- OCR 교차 리뷰: Medium 3건 — ① URL 오버라이드가 프로덕션 빌드에 포함 → **유지**, 배포 데모에서 재현 가능해야 함. ② 필터 0건에 컬럼이 통째로 사라짐 → **유지**, 문장 하나가 5개 반복보다 명확. ③ `?seed=0`/`?reset`이 URL로 데이터를 지움 → **유지**, mock이라 손실 비용 0. Low 2건 버림.
- HTML diff 리뷰: 파일 10개.

### 판단

- ErrorBoundary 직접 작성, 오버라이드 프로덕션 포함, 0건이면 컬럼 교체
