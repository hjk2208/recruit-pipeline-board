# 설계 결정 기록

형식: `- <날짜> 주제: 결정 — 근거 (기각한 대안)`

## 결정

- 2026-09-11 작업 범위: **6시간 안에 Must 6개 + 기록을 완결**하는 것을 목표로 잡음 — Should는 Must가 전부 검증된 뒤 남는 시간에만. 완성도보다 각 기능의 판단 과정이 드러나는 쪽을 우선.
- 2026-09-11 프레임워크: **Vite + React + TS** — 페이지 하나, SSR·라우팅·API 라우트 불필요. Vitest 통합이 가장 가볍고 HMR·빌드가 빠름. (Next.js — 이 요구사항에서 얻는 게 없고 `'use client'` 경계·테스트 셋업 비용만 추가라 기각)
- 2026-09-11 서버 상태: **TanStack Query** — 캐시·재조회·로딩/에러 경계를 표준 방식으로 처리. 낙관적 업데이트의 스냅샷·복원 로직은 `onMutate`/`onError` 안에 **직접 작성**하므로 "핵심 상태 로직을 통째로 대신하는 라이브러리"에 해당하지 않는다고 판단. (zustand/useReducer로 서버 상태까지 직접 관리 — 제어력은 높지만 로딩·에러·재조회를 전부 수작업, 시간 대비 버그 위험이 커서 기각)
- 2026-09-11 클라이언트 상태: `useState` 우선, 여러 컴포넌트가 공유할 때만 **zustand**. 서버 데이터는 zustand에 넣지 않음 — 상태 출처를 한 곳으로.
- 2026-09-11 단계 이동 UI: **명시적 액션 버튼** — 키보드 접근성이 기본으로 확보되고, 드래그앤드롭 라이브러리·키보드 대체 경로 구현 비용이 사라짐. (드래그앤드롭 — UX는 좋지만 키보드 대체 경로 필수 조건 때문에 구현량 2배, 기각)
- 2026-09-11 mock API: **모듈로 자체 구현** (`Promise` + `setTimeout` + 난수, localStorage 영속) — 지연·실패율을 코드로 통제해 테스트에서 주입 가능. (MSW — 실제 fetch 경로를 유지할 수 있지만 서비스워커 셋업 비용이 있고, 요구사항이 "자체 구현"이라 기각)
- 2026-09-11 API 계층: **인터페이스 + mock 구현체로 분리** — 화면·훅은 인터페이스만 의존, 실제 백엔드가 붙으면 구현체만 교체. 지연·실패율은 mock 안에만. (mock을 한 파일에 몰아 짜기 — 빠르지만 교체 시 훅까지 손대야 해서 기각)
- 2026-09-11 단계 이동 컨트롤: 카드 안 `<select>` — 어느 단계에서든 불합격 처리 가능, 네이티브라 키보드 접근성 기본. (이전/다음 버튼 — 불합격 전용 버튼 필요, 클릭 수 증가로 기각)
- 2026-09-11 상세 보기: PC는 사이드 패널(보드를 보면서 카드를 바꿔가며 열람·이동하는 동선), 모바일(<768px)은 전체 화면 시트 — 같은 컴포넌트가 뷰포트로 형태만 전환. (모달 단일 — PC에서 보드가 가려져 훑기 리듬이 끊김. 기각)
- 2026-09-11 반응형: PC 우선. 모바일은 컬럼 가로 스크롤(스냅)로 깨지지 않는 수준까지만 — 담당자 도구는 데스크톱 사용이 기본.
- 2026-09-11 seed 규모: 250건으로 시작 — Must "200건 이상" 충족. 1,000건은 추가 요구사항(가상 스크롤) 단계에서 전환.
- 2026-09-11 세부 값: 직무는 요구사항에 없어 프론트엔드/백엔드/디자인/PM/데이터 5종으로 정함(seed와 필터 옵션 공통). 지원일은 YYYY-MM-DD. mock 실패율은 쓰기 15%·조회 5% — 조회까지 15%면 첫 화면이 자주 깨져 작업 흐름이 끊김, 에러 상태는 재시도로 검증.
- 2026-09-11 mock API 반환값: `moveCandidate`는 갱신된 지원자 하나를 돌려준다 — 캐시에서 그 항목만 교체, 실백엔드 `PATCH /:id` 응답과 같은 모양. (전체 목록 반환 — 구현은 쉽지만 250건 재전송, 교체 시 훅을 다시 짜야 해서 기각)
- 2026-09-11 에러 형태: `ApiError { code, status, retryable }` — `retryable`로 토스트의 재시도 버튼 노출을 결정, 실백엔드에선 HTTP 상태를 매핑. (메시지만 담은 Error — 재시도·충돌 구분이 안 돼 기각)
- 2026-09-11 지연 방식: 호출마다 200~800ms 랜덤, 범위·난수·저장소·시계를 옵션으로 주입 — 테스트는 지연 0 + 고정 수열로 결정적. (인스턴스당 고정 지연 — 시뮬레이션 의미가 약해 기각)
- 2026-09-11 롤백 단위: 항목 단위 — 실패한 카드의 이전 단계만 복원. 전체 목록 스냅샷 복원은 다른 카드의 진행 중인 낙관적 상태까지 지워서 Must 3 자체가 깨짐. (6번 단위에서 이 결정대로 구현)
- 2026-09-11 mock 에러 재현 스위치: 9번 단위로 이관 — 에러·빈 상태 화면 확인에 필요하지만 mock-api 단위 범위 밖. `to` 런타임 검증은 기각 — 호출자가 전부 타입 안전한 내부 코드.
- 2026-09-11 폴더 구조: `app/`(Provider 조립) + `features/<단위>/` — 작업 단위와 폴더가 1:1이라 커밋 scope로 파일을 찾을 수 있음. 공통 컴포넌트 폴더는 같은 패턴 3번째에 생성. (평평한 components/ + hooks/ — 파일 10개 이하면 단순하지만 단위 11개라 흩어짐. 기각)
- 2026-09-11 Query 재시도: `retry: false` — mock 실패 15%가 라이브러리 자동 재시도에 가려지면 롤백을 확인할 수 없음. 재시도는 사용자가 누르는 버튼으로.
- 2026-09-11 카드 포커스 대상: 카드 전체가 아니라 **이름 버튼** — 카드 전체를 button으로 만들면 단계 이동 select를 안에 넣을 수 없음(인터랙티브 중첩). 이름 버튼이 상세 열기, select는 형제 요소. (div + tabIndex + role=button — 네이티브 버튼이 주는 Enter/Space·포커스 링을 손으로 짜야 해서 기각)
- 2026-09-11 AI 출력 검증: **OCR(open-code-review) delegate 모드로 교차 리뷰** — 구현한 AI와 다른 규칙셋으로 diff를 다시 보게 함. 자동 수정은 끄고 리포트만 받아 채택·기각을 사람이 결정. API 키 불필요.
- 2026-09-11 AI 협업 규칙: 모호한 요구사항은 AI가 가정하지 않고 **선택지·트레이드오프를 제시 → 사용자가 결정 → 이 파일에 기록**. 설계 판단의 주체를 사람으로 고정.

## AI 제안 중 채택 / 기각

- 기각: 서버 상태를 직접 스토어로 관리하자는 초기 제안 — 위 "서버 상태" 항목 참조.
- 기각: AI가 모호한 요구사항을 스스로 가정하고 기록하는 규칙 — 판단 주체가 AI가 되어 버려서 사용자 결정 방식으로 바꿈.

## 못 끝낸 기능

(마무리 시 작성)
