# 작업 규칙

파일/폴더 생성과 코드 작성은 에이전트가 수행한다. 사용자는 판단·확인·피드백을 담당한다.

## 스택 (확정)

- Vite + React + TypeScript, Tailwind CSS v4, Vitest + Testing Library, oxlint.
- **서버 상태는 TanStack Query.** 컴포넌트 상태는 `useState`. 여러 컴포넌트가 공유해야 하는 클라이언트 상태만 zustand. **서버 데이터를 zustand에 넣지 않는다.**
- Next.js는 쓰지 않는다 — 페이지 하나, SSR·라우팅 불필요.

## 제약 (요구사항에서 온 것, 타협 불가)

- **mock API는 자체 구현.** 지연 200~800ms, 실패율 약 15%를 시뮬레이션. 새로고침 후에도 데이터가 유지되어야 한다.
- **단계 이동은 드래그앤드롭이 아니라 명시적 버튼.** 키보드만으로 이동·상세 열람이 가능해야 한다.
- **낙관적 업데이트와 실패 시 롤백은 직접 작성한다.** 스냅샷·복원 로직을 라이브러리에 위임하지 않는다. TanStack Query의 `onMutate`/`onError`는 훅 자리일 뿐, 그 안의 로직은 우리가 쓴다.

## 구현 리듬

- **새 코드를 쓰기 전에 재사용을 먼저 확인한다**: 이미 있는 코드 → 표준 라이브러리/플랫폼 기능 → 설치된 의존성 순. 없을 때만 최소한으로 구현.
- **한 번에 한 단위씩.** 여러 기능을 한 번에 요청받아도 단위로 나눠 순서를 제안하고 하나씩 진행한다. 1단위 = 미리보기 확인이나 테스트 실행 한 번으로 검증 가능한 크기.
- **핵심 로직은 테스트를 먼저 쓴다.** 인수 기준을 테스트로 옮긴 뒤 통과할 때까지 구현-실행을 반복한다. 통과 못 하는 원인이 요구사항 해석 차이면 멈추고 사용자에게 묻는다.
- **모든 단위는 구현 직후 `/verify`로 검증한다** (타입체크·린트·테스트·빌드). 브라우저 확인이 필요하면 사용자에게 요청한다 ("미리보기에서 X 해보세요"). 검증 없이 다음 단위로 넘어가지 않는다.
- 시간·확률·환경 의존 로직(지연, 실패율 등)은 **검증 가능하게 만든다** — 순수 함수 분리, 의존성 주입, 테스트 작성. 이유와 함께 기록.
- **라이브러리 설치 전 `npm view <패키지> version`으로 최신 안정 버전을 확인**하고 그 버전을 고정 설치한다. 기억에 의존하지 않는다.
- **단위 완료의 기준은 "사용자가 화면에서 실제로 쓸 수 있다"이다.** 로직만 있고 UI에 연결되지 않았으면 미완료.
- 단위가 끝나면 `/unit-done`으로 마감한다 (검증 → 리뷰 → 기록 → 커밋 → push).

## 기록

- **PROMPTS.md** — 기능마다 한 섹션. `## [scope] 제목` 아래에 프롬프트 원문(요약 아님) / AI 출력 요지 / 리뷰·검증(무엇이 문제였나·어떻게 알아챘나·채택/수정/기각과 이유) / 판단. **해당 기능 커밋에 같이 포함한다.** 사소한 오타 수정 프롬프트는 생략 가능.
- **DECISIONS.md** — 설계 결정은 **결정 즉시** 한 줄씩. 몰아서 쓰지 않는다. 형식: `- <날짜> 주제: 결정 — 근거 (기각한 대안)`.
- 같은 유형의 피드백이 반복되면 개별 수정에 그치지 말고 규칙으로 만들어 이 문서에 추가할 것을 제안한다.

## 커밋

- **기능 하나 = 커밋 하나.** 여러 기능을 한 커밋에 섞지 않는다.
- 메시지: `type(scope): 요약` + 빈 줄 + 본문(무엇을·왜, AI 초안을 어떻게 손봤는지 한 줄 이상). type은 `feat/fix/refactor/test/docs/chore`.
- **squash·force-push 금지.** 시행착오 커밋도 그대로 남긴다.
- 커밋은 사용자가 메시지를 확인한 뒤 실행하고, **커밋 직후 push한다.**

## 코드 스타일

- **비동기 상태는 컴포넌트 안에서 `isLoading`/`isError` 분기하지 않는다.** 로딩은 Suspense, 에러는 ErrorBoundary 경계에 위임하고, 데이터 소비 컴포넌트는 성공 상태만 다룬다. 서버 상태는 `useSuspenseQuery`로 소비한다.
- **파일은 책임 단위로 분리한다.** 역할을 한 문장으로 설명할 때 "그리고"가 들어가면 분리. 컴포넌트/훅/순수 유틸/데이터/타입은 서로 다른 책임. 컴포넌트는 파일당 하나.
- **스타일은 Tailwind.** 색·간격 등 토큰은 `@theme`에 정의하고 정의된 유틸만 사용(임의 값 남용 금지). 공통 컴포넌트는 같은 패턴이 3번 반복될 때 추출한다.
- **인터랙티브 요소(버튼·카드·입력)에는 hover/active/focus-visible 피드백**을 넣고 transition으로 처리한다. focus 링을 제거하지 않는다.
- 외부 네트워크 의존(원격 이미지·CDN)은 피한다.

## 소통

- **요구사항이 모호하면 임의로 정하지 않는다.** 선택지와 트레이드오프를 제시하고 사용자가 결정한다. 결정과 그 가정은 DECISIONS.md에 기록.
- **중요한 diff는 커밋 전에 HTML 뷰로 보여준다.** 핵심 로직(mock API, 낙관적 업데이트·롤백, 상태 스토어)은 크기 무관하게 항상, 그 외는 50줄 이상이거나 파일 3개 이상일 때. 사용자가 실제로 읽고 판단한 내용이 PROMPTS.md 리뷰 항목이 된다.
- 스코프를 넘는 작업은 제안만 하고 착수하지 않는다. **필수 요구사항이 전부 완료·검증되기 전에는 범위 확장에 착수하지 않는다.**
